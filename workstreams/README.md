# Workstreams

Each workstream is an independently reviewable unit of mathematical research.

Required files:

- `instructions.md`
- `status.json`
- `report.md`
- `review/`
- `artifacts/`
- `logs/`

Only the project coordinator should create new workstream directories. Workstream coordinators should keep edits inside their assigned directory.


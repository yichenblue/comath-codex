# Goals

Create `goal_001.md`, `goal_002.md`, and so on with `scripts/approve_goal.py` after explicit user approval.

New goals should start as `DRAFT` and become `APPROVED` only through the goal approval gate.
